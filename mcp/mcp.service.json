{
    "name": "CustomerManagementService",
    "description": "Handles creation, retrieval, and updates of customer records.",
    "methods": [
      {
        "name": "createCustomer",
        "description": "Creates a new customer using provided name, email, and phone number.",
        "parameters": {
          "type": "object",
          "properties": {
            "name": { "type": "string" },
            "email": { "type": "string" },
            "phone": { "type": "string" }
          },
          "required": ["name", "email"]
        }
      },
      {
        "name": "getCustomerByEmail",
        "description": "Retrieves customer details based on email address.",
        "parameters": {
          "type": "object",
          "properties": {
            "email": { "type": "string" }
          },
          "required": ["email"]
        }
      }
    ]
  }
  